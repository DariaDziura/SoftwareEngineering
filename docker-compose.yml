services:
  web:
    build:
      context: ./
    volumes:
      - .:/src
    command: supervisor -e 'js,pug,html' --exec node index.js
    ports:
      - "3000:3000"
    depends_on:
      - db

  db:
    image: mysql:latest
    restart: always
    env_file:
      - ./.env
    command: --innodb_use_native_aio=0
    ports:
      - "3308:3306"
    volumes:
      - ./db:/var/lib/mysql
      # ADD THIS LINE BELOW:
      - ./sd2-db.sql:/docker-entrypoint-initdb.d/init.sql
    # This ensures the SQL script runs only on the very first startup

  phpmyadmin:
    image: phpmyadmin/phpmyadmin:latest
    restart: always
    env_file:
      - ./.env
    ports:
      - "8081:80"
    depends_on:
      - db