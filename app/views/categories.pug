extends layout

block content
  h1 Categories

  ul
    each category in categories
      li
        //- Change this line to match your /category/:id route
        a(href=`/category/${category.id}`)= category.name